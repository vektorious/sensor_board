__pycache__/
*.pyc
.venv/
venv/
.env
*.log
app/data/*.db
app/data/*.db-wal
app/data/*.db-shm
# Init lock (app/database.py): created next to the database at startup, so it
# lands in the same directory as the ignored .db files but did not match them.
app/data/*.init.lock
.idea/

CLAUDE.md

# Internal design notes — not for publication
docs/future-auth-model.md

# Local development notes — not for publication
notes/

# Impressum / privacy fragments: operator-specific, and they carry a real name
# and postal address. Published per instance, never in the repository.
# See app/legal.py and DEPLOY.md.
app/legal/

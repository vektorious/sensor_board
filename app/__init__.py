"""Sensor Board application package."""

# Semantic version. 0.x means the ingestion contract may still break between
# minor releases; see CHANGELOG.md. Bump to 1.0.0 once the contract is frozen.
__version__ = "0.2.0"
